import { connect } from 'react-redux'
import SwapQuotes from './swap-quotes.component'

const mapStateToProps = (state) => ({})

const mapDispatchToProps = (dispatch) => ({})

export default connect(mapStateToProps, mapDispatchToProps)(SwapQuotes)
