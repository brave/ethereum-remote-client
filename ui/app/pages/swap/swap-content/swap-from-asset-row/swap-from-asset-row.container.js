import { connect } from 'react-redux'
import SwapFromAssetRow from './swap-from-asset-row.component'

function mapStateToProps () {
  return {}
}

function mapDispatchToProps () {
  return {}
}

export default connect(mapStateToProps, mapDispatchToProps)(SwapFromAssetRow)
